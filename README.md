# Microtonal Init Presets For Reason

This repo contains presets for multiple Reason Rack devices, grouped by tuning.

Add this folder to ~/Music/Reason Studios/User Library

Tweak tunings and settings in `config.py`

Run with: `uv run generate-presets`
